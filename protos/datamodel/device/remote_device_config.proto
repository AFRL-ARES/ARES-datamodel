syntax = "proto3";

package ares.datamodel.device;

message RemoteDeviceConfig {
	optional string unique_id = 1; // A unique id for this device
	string name = 2; // name of the device
	string url = 3; // the connection url of the device
}